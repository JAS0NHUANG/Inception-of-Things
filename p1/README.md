# p1

## Vagrant

Vagrant is a tool used to manage, setup vertual machines.

- Vagrantfile

- Vagrant commands cheat sheet:
`vagrant up`
`vagrant ssh [vm-name]`
`vagrant destroy -f`
`vagrant validate`


## K3s

K3s is a simplified version of Kubernetes. (k8s) 
A container orchestration tool.

- installation
server:
workder:

- k3s commands cheat ssheet:
kubctl


